# Consistency review (workflow wmkstjc57) — 30 adversarially-confirmed findings

5 auditors (logic/terminology/numerics/overclaim/narrative) + 37 verifiers; synth agent
hit the StructuredOutput size limit so findings were extracted from transcripts directly.

## [CRITICAL] §9 Practical Guidance, item 3 (lines 557-561): "**Choose the construction operation by imprinting character, not by default-priming.** For extraction with a known task: on a high-imprintability token-presence model (Gemma), conditioning *hurts* ... on a weak/surface imprinter where selection *hurts* (Qwen-7B), **condition** instead." — contradicts §7.1 (lines 512-516): "we therefore **withdraw** the earlier 'decision rule indexed by imprinting character'; the deployable prescription is the weaker but honest *cheaply probe both operations per model* ... not a trait shortcut."
- issue: Direct self-contradiction that is fatal to the narrative's central honesty claim. §7.1 explicitly withdraws the rule that maps imprinting character -> which construction operation to use (justified by r=0.29 with imprintability, and by Gemma-1B and Qwen-14B sitting at nearly equal imprintability with opposite outcomes). Yet the Practical Guidance section — the section a practitioner reads first — reinstates precisely that withdrawn rule and even re-uses the 'surface imprinter' label that §6.5 (line 397, 417) retired. A skeptical reviewer will treat this as evidence the authors do not believe their own retraction, undermining the paper's entire 'methodological honesty' framing.
- fix: Rewrite §9 Practical Guidance item 3 to match §7.1 and §10 — do NOT key the operation to imprinting character, and drop the "weak/surface imprinter" label. Replace with, e.g.:

"3. **When you know the task, probe both construction operations per model — do not assume one.** Which of task-aware *selection* (keep relevant tokens) vs. *conditioning* (discarded-task prime) wins is model-specific and does NOT reduce to imprintability (r=0.29 with conditioning value) or size: Gemma-1B and Qwen-14B sit at nearly equal imprintability yet have opposite outcomes. Run one cheap N~300 sweep of both operations before deploying. The one robust prior is negative: aggressive query-aware selection carries a Qwen-family-specific penalty (+0.9 to +1.9 nats, controlled for answer-span survival) that never appears on Gemma and that discarded-prefix conditioning avoids — so if you must deploy a single operation blind on a Qwen model, prefer conditioning over SnapKV-style selection."

This keeps the genuinely supported empirical prior (the Qwen selection penalty, §7.1 third reading) and the warning against blind single-operation deployment, while removing the withdrawn imprinting-character law and the retired surface-imprinter framing.

## [CRITICAL] §9 Practical Guidance, item 3, line 557: "**Choose the construction operation by imprinting character, not by default-priming.** For extraction with a known task: on a high-imprintability token-presence model (Gemma), conditioning *hurts* — prefer task-aware **selection** ... on a weak/surface imprinter where selection *hurts* (Qwen-7B), **condition** instead."
- issue: §9.3 resurrects the exact decision rule that §7.1 explicitly WITHDRAWS. §7.1 line 514 says: "We therefore **withdraw** the earlier 'decision rule indexed by imprinting character'; the deployable prescription is ... *cheaply probe both operations per model*, not a trait shortcut." The abstract (line 46, "we withdraw the 'rule indexed by imprinting'") and conclusion (line 607, "probe both per model") agree with §7.1. §9.3 directly contradicts all three by telling the reader to choose the operation by imprinting character. Worse, the rule as stated is refuted by the paper's own data: it says "on a high-imprintability token-presence model (Gemma), conditioning hurts," but Gemma-1B (a Gemma) has the LARGEST conditioning HELP (full conditioning −1.91*, §7.1 table); and it says a weak imprinter where selection hurts should "condition instead," but selection hurts ALL Qwens including Qwen-14B, whose conditioning HURTS most (+2.37*) — the exact counterexample §7.1 uses to withdraw the rule.
- fix: Rewrite §9 Practical Guidance item 3 (paper_draft_v5.md lines 579-583) to match §7.1/abstract/conclusion/limitations. Replace with, e.g.: "**Do not choose the construction operation by a trait shortcut — probe both per model.** For extraction with a known task, neither task-aware selection (keep relevant tokens) nor discarded-prefix conditioning dominates, and which wins does not reduce to imprintability (r=0.29) or size — Gemma-1B (conditioning helps most, -1.9) and Qwen-14B (conditioning hurts most, +2.4) sit at nearly the same imprintability. Run one cheap (N approximately 300) per-model sweep of both operations rather than assuming one. The only reliable prior is confound-controlled on the selection side: aggressive query-aware (SnapKV-style) selection carries a Qwen-family penalty (+0.9 to +1.9 nats, even at matched answer-span survival), so avoid selection-only pipelines on Qwen; conditioning sidesteps this." Remove the "by imprinting character" framing and the false "on Gemma conditioning hurts" / "on Qwen condition instead" assignments entirely.

## [CRITICAL] Section 9 point 3 lines 557-561
- issue: Section 9.3 reinstates the per-family rule that section 7.1 withdraws, falsified by Gemma-1B where conditioning helps most at minus 1.91.
- fix: Rewrite Practical Guidance point 3 to match §7.1's actual conclusion instead of reinstating the withdrawn trait rule. Drop the "by imprinting character" framing and the blanket "on Gemma, conditioning hurts" claim. Replace with: (a) the one systematic, confound-controlled result — aggressive query-aware (SnapKV-style) selection hurts the whole Qwen family (+0.9 to +1.9 nats) but never Gemma, so selection carries a real Qwen-specific risk; and (b) for the select-vs-condition choice generally, state that neither operation dominates and which wins does not track imprintability or size (r=0.29; e.g., Gemma-1B is the strongest conditioning help at -1.91 while similar-imprintability Qwen-14B is the strongest hurt at +2.4), so run one cheap N~300 per-model probe rather than picking by family/trait. This keeps 9.3 consistent with §7.1 (lines 534-539) and §10 (lines 601-604).

## [MAJOR] §2 Related Work, line 132: "Our contribution to this thread is the *mode* account: which conditioning content a given model can bank is governed by a measurable trait (§6.2)"; and §4, line 232: "foreshadows the imprinting-mode thesis (§6)"; and §2, line 115: "our bankability ceiling (§5) and imprinting-mode result (§6)"
- issue: Three residual uses of the retracted "mode" framing in prose. §6.5 renamed the central result from an "imprinting mode" to a "content-imprint axis" (magnitude) plus a model-specific token-presence-vs-structure character, and states there is no clean mode. Calling §6 "the *mode* account"/"imprinting-mode thesis"/"imprinting-mode result" mislabels the corrected §6 with the withdrawn vocabulary.
- fix: Line 137: replace "Our contribution to this thread is the *mode* account:" with "Our contribution to this thread is the *content-imprint* account:". Line 232: replace "foreshadows the imprinting-mode thesis (§6)" with "foreshadows the content-imprint thesis (§6)". Line 120: replace "imprinting-mode result (§6)" with "content-imprint result (§6)". Retain the word "mode" only at the sites that explicitly negate it (lines 25, 85, 276, 410, 438, 548, 623). These three substitutions match the paper's own §6.1 header ("a content-imprint axis") and Introduction point 2 ("A content-imprint axis"). (Note: the correct line numbers are 137, 232, 120 — not the 132/115 given in the finding — but the quoted strings are unique so the edits are unambiguous.)

## [MAJOR] §2 Related Work, line 132-134 ("Our contribution to this thread is the *mode* account: which conditioning content a given model can bank is governed by a measurable trait (§6.2), so the right task-aware prime is *model-specific*") and line 142-143 ("we introduce a *what-kind* axis — a content-type dissociation (meaning vs. surface form) that is model-specific")
- issue: §2 Related Work still frames the paper's contribution in the retracted vocabulary. (a) 'the *mode* account: which conditioning content ... is governed by a measurable trait' contradicts §6.5, which says the trait predicts only MAGNITUDE and 'the *kind* of banking is model-specific' NOT trait-governed (line 315), and §7.1, which withdraws the trait-indexed prescription (r=0.29). (b) 'the right task-aware prime is *model-specific* — a prescription these retention-based methods do not provide' advertises a prescription §7.1 explicitly withdraws. (c) 'a content-type dissociation (meaning vs. surface form)' is exactly the 'semantic vs. surface' dichotomy §6.5 retracts ('no clean two-way semantic/surface mode', line 417; 'We retract the stronger "Gemma banks meaning" reading', line 426).
- fix: In §2 (Related Work), align the contribution statements with the corrected thesis in §1/§6.2/§6.5/§7.1:

1. Replace the "Task-aware and trained caches" closing sentence (line 136-138): "Our contribution to this thread is the *mode* account: which conditioning content a given model can bank is governed by a measurable trait (§6.2), so the right task-aware prime is *model-specific* — a prescription these retention-based methods do not provide."
   with: "Our contribution to this thread is a content-imprint *magnitude* axis — how much a model banks is predicted by a measurable trait (imprintability, r=0.94; §6.2) — plus a separate, model-specific *kind* characterization (token presence vs. structure; §6.5). Because neither conditioning nor selection dominates and the choice does not reduce to that trait (r=0.29; §7.1), the honest task-aware prescription is to *probe both operations per model* rather than the trait shortcut — a step these retention-based methods do not address."

2. Replace the "what-kind" sentence (line 146-147): "we introduce a *what-kind* axis — a content-type dissociation (meaning vs. surface form) that is model-specific and, we show, *trainable* (§6.4)."
   with: "we introduce a *what-kind* axis — a token-presence-vs-structure dissociation that is model-specific and, we show, *trainable* (§6.4)."

This removes the retracted "*mode* account," the trait-governed-content claim, the withdrawn prescription framing, and the "meaning vs. surface form" dichotomy, matching §6.5 (line 437-448) and §7.1 (line 533-538).

## [MAJOR] §9 Practical Guidance, point 3, line 557: "**Choose the construction operation by imprinting character, not by default-priming.**"
- issue: This prescription contradicts §7.1, which explicitly WITHDRAWS exactly this rule: line 514 "we therefore **withdraw** the earlier 'decision rule indexed by imprinting character'" and line 511-512 "the choice does *not* reduce to a single trait ... (r=0.29)". §10 Limitations (line 575) also states the comparison "does not yield a predictive rule." Point 3 reinstates the withdrawn trait-indexed rule as deployable guidance and even routes by "weak/surface imprinter" (surface = retracted mode vocab, line 559).
- fix: Rewrite §9 point 3 to match §7.1's and §10's honest verdict. Replace the heading "Choose the construction operation by imprinting character, not by default-priming." with something trait-agnostic, e.g. "**When you know the task, probe both operations per model — do not assume selection or conditioning.**" Body: state that across eight models neither operation dominates (conditioning helps four, hurts four) and which wins does NOT reduce to imprintability (r=0.29) or size — e.g. Gemma-1B (low imprintability) is the strongest conditioning winner while similarly-imprintable Qwen-14B is the strongest loser — so deployment requires a cheap per-model N≈300 select-vs-condition sweep. Remove "by imprinting character," the "high-imprintability token-presence model (Gemma) → prefer selection" routing (contradicted by Gemma-1B in the paper's own table; if kept, scope it to the larger Gemmas, not "Gemma"), and the retracted "weak/surface imprinter" phrasing. Retain only the genuinely systematic, confound-controlled claim that aggressive query-aware selection hurts the entire Qwen family (not Gemma) even at matched answer-span survival, so conditioning is the safer default specifically for Qwen.

## [MAJOR] §6 header + boxed note (lines 268-276): "## 6. Imprinting: a Content-Imprint Axis, and What It Actually Banks (the central result)" immediately followed by "> **Reading guide / correction.** An earlier framing of this section called the headline axis 'semantic imprinting' and split models into 'semantic vs. surface' *modes*. Word-order shuffle controls (§6.5) show that label is too strong ..."
- issue: The central results section opens by narrating its own prior draft's error before stating the current result. Placing a 'correction' box at the top of the flagship section makes the visible course-correction structural rather than incidental — it reads as a changelog entry, not a paper. A reviewer encountering 'the central result' section that begins by apologizing for a previous framing will discount the section before reading the evidence.
- fix: Remove the standalone "**Reading guide / correction.**" box at the top of §6 (lines 275-281) and let §6.1 open the section, since §6.1's heading already states the corrected thesis and its body (lines 296-308) already carries the full retraction. Optionally retitle §6 to drop the self-aggrandizing "(the central result)" parenthetical, or fold the one genuinely useful pointer from the box — "magnitude results are robust; the *kind* of banking is reinterpreted in §6.5" — into the first sentence of §6.1 as a forward reference rather than a boxed retraction. Do NOT reduce the correction to a single footnote at §6.5: this paper's contribution is explicitly framed as an honest self-correction (abstract line 9, §1.2, conclusion), so the retraction should remain visible in the abstract, §6.1, and §6.5 — the fix is only to eliminate the duplicated top-of-section banner, not the correction narrative itself.

## [MAJOR] §7 header, line 430: "## 7. Mode–Task Match: When Imprinting Helps"
- issue: The most prominent surviving instance of the RETRACTED "mode" vocabulary. The section is titled "Mode–Task Match," but its own body (line 456 "we therefore demote the mode–task '2×2' below to an *illustrative* worked pair") and §6.5 (line 417 "there is, in short, **no clean two-way semantic/surface mode**") explicitly reject the existence of a mode and of a mode–task match. A reader scanning headers gets the withdrawn framing as the section thesis.
- fix: Rename the §7 header to drop the retracted term, e.g. "## 7. When Imprinting Helps: Downstream Value and Task-Aware Construction". Also fix residual un-caveated instances of the retracted vocabulary: (a) the Figure 6 caption at line 491, change "Mode–task match for one worked pair (Gemma-12B, Qwen-7B)" to "Illustrative worked pair (Gemma-12B, Qwen-7B)"; (b) optionally keep the in-body "mode–task '2×2'" at line 478 since it is already scare-quoted and explicitly demoted. The intro references at lines 43/86 are already scare-quoted as "the mode–task 'match' of earlier drafts is weaker than claimed," so they are acceptable as-is.

## [MAJOR] Section 2 lines 142-144 and 132-134
- issue: Sells the retracted meaning-vs-surface dissociation and a withdrawn model-specific prescription.
- fix: Bring the two Section 2 Related-Work sentences in line with the paper's own retractions. (1) At ~line 137-139 replace "Our contribution to this thread is the *mode* account: which conditioning content a given model can bank is governed by a measurable trait (§6.2), so the right task-aware prime is *model-specific* — a prescription these retention-based methods do not provide." with something like: "Our contribution to this thread is twofold: a content-imprint *axis* whose magnitude one trait predicts (§6.2), and an honest select-vs-condition comparison showing neither operation dominates and that which wins does not reduce to that trait (r=0.29) — so the deployable prescription is to probe both operations per model (§7.2), which retention-based methods do not address." (2) At ~line 147-150 drop the disavowed parenthetical: change "a content-type dissociation (meaning vs. surface form) that is model-specific and, we show, *trainable* (§6.4)" to "a content-imprint axis (magnitude, §6.2) whose banked content *type* is model-specific and, we show, *trainable* — set by instruction-tuning (§6.4)", preserving the genuine §6.4 base→instruct content-type-flip result while removing the "meaning vs. surface form" mode label.

## [MAJOR] Appendix B, line 638: "*Select-vs-condition (§7.1), N=300, k=32:* selVal Qwen-7B +1.00\* (else n.s.)"
- issue: "(else n.s.)" is directly contradicted by the §7.1 body table (lines 489-496) and the source of truth: selVal is statistically significant for 6 of 8 models, not just Qwen-7B. Qwen-1.5B +0.88*, Qwen-3B +1.89*, Qwen-14B +0.88*, Gemma-1B −0.33*, Mistral-7B +0.50* are all starred; only Gemma-4B (−0.12) and Gemma-12B (−0.03) are n.s. The Appendix claim that all non-Qwen-7B selVals are non-significant is false and undercuts the paper's own headline that selection systematically hurts the whole Qwen family.
- fix: Replace "selVal Qwen-7B +1.00\* (else n.s.);" (line 667) with an accurate summary matching the source of truth and the §7.1 table, e.g.: "selVal (selection cost) Qwen-1.5B/3B/7B/14B +0.88\*/+1.89\*/+1.00\*/+0.88\*, Mistral-7B +0.50\*, Gemma-1B −0.33\* (all CIs exclude 0); Gemma-4B −0.12 / Gemma-12B −0.03 n.s.;". This preserves the compact Appendix style while making the significance flags consistent with lines 512-519 and reinforcing (rather than contradicting) the whole-Qwen-family selection-cost headline.

## [MAJOR] §3.3 line 201 ("Eight instruction-tuned models spanning imprintability: Qwen 2.5 (1.5/7/14B), Mistral 7B, Gemma 3 (1/4/12/27B)") vs §7.1 table (line 490 "Qwen-3B") and Abstract line 44 ("Qwen-1.5B/3B/7B")
- issue: There are two different "eight-model" sets called "eight models" throughout. The r=0.94 imprintability analysis (§6.2) and §3.3 use {Qwen 1.5/7/14B, Mistral, Gemma 1/4/12/27B}. The select-vs-condition analysis (§7.1, Fig 8, abstract line 43-44) uses {Qwen 1.5/3B/7/14B, Mistral, Gemma 1/4/12B} — it ADDS Qwen-3B and DROPS Gemma-27B. Qwen-3B is used in §7.1/Fig8/abstract but is never listed in §3.3's Models section, so a reader cannot resolve where Qwen-3B came from, and "eight models" is used for two non-identical sets.
- fix: In §3.3, expand the model roster to include Qwen-3B and flag that the panels differ, e.g.: "Eight instruction-tuned models spanning imprintability: Qwen 2.5 (1.5/3/7/14B), Mistral 7B, Gemma 3 (1/4/12/27B). The imprintability/shuffle analyses (§6) use the 1.5/7/14B Qwens and add Gemma-27B; the task-aware select-vs-condition sweep (§7.1) instead uses Qwen-3B in place of Gemma-27B (the compute-heaviest model), so each 'eight-model' panel is a size-matched subset rather than an identical set." Alternatively, add Qwen-3B to the §3.3 list and add a one-line footnote to the §7.1 table and the abstract's 'eight models' noting that §7.1 substitutes Qwen-3B for Gemma-27B relative to the §6.2 imprintability panel. No results change; this is purely reconciling the roster description.

## [MAJOR] Abstract, lines 22-23: "it recovers up to ~36% of a semantic context's value from a stripped cache (−3.8 nats on Gemma 27B), rising monotonically in nats across the Gemma family"
- issue: The abstract binds the peak fraction (~36%) to Gemma-27B via the parenthetical "(−3.8 nats on Gemma 27B)". But §6.2 (line 333) states the fraction is "6% → 23% → 36% → 35% (1B→4B→12B→27B), peaking at 12B" — i.e., ~36% is the 12B value and 27B is 35%. My recomputation from exp26 confirms 12B≈37.6% and 27B≈32.8%, so the peak is 12B, not 27B. The −3.8 nats (src gemma27b sem −3.770) is correctly 27B's ABSOLUTE banking, but 27B's FRACTION is ~33-35%, not 36%. The abstract conflates 12B's peak fraction with 27B's absolute nats, contradicting the body.
- fix: Decouple the peak fraction from Gemma-27B so the abstract matches the body (§6.2, "peaking at 12B"). Replace lines 22-23:

"it recovers up to ~36% of a semantic context's value from a stripped cache (−3.8 nats on Gemma 27B), rising monotonically in nats across the Gemma family,"

with:

"it recovers up to ~36% of a semantic context's value from a stripped cache (peaking at Gemma-12B; the largest absolute effect is −3.8 nats on Gemma-27B), rising monotonically in nats across the Gemma family,"

This preserves the accurate ~36% peak fraction and the accurate −3.8-nat/27B absolute figure while removing the false implication that ~36% is 27B's fraction.

## [MINOR] Abstract lines 22-24 and section 6.4 lines 362-363
- issue: Couples 36 percent (a 12B figure) with minus 3.8 nats on Gemma 27B (a 27B figure); and universal and flip claims rest on n 3 base models and n 1 flipping model, uncaveated in section 10.
- fix: Two independent fixes in /home/jupyter/research/directed_kvcache_publication/paper_draft_v5.md:

1) Decouple the abstract's metric/model mismatch (lines 22-23). Either report both figures for one model — e.g. "recovers up to ~36% of a semantic context's value (−3.6 nats on Gemma 12B)" — or state them as two clearly-separate maxima: "recovers up to ~36% of a semantic context's value (peaking on Gemma 12B; the absolute banking peaks at −3.8 nats on Gemma 27B)". Do not attach the 27B nats number to the 12B 36% fraction as one point.

2) Add a one-line caveat to §10 (Limitations) noting the sample basis: the "universal property of pretrained LMs" claim rests on three base models (Gemma-4B, Qwen-7B, Mistral-7B), and the base→instruct inversion ("flip") is demonstrated on a single model (Qwen 2.5); broader base-model coverage and a second inverting family would strengthen both. Optionally soften "universal" at line 368 to e.g. "consistent across all three base models tested".

## [MINOR] §4, line 238: "only qwen-1.5B shows the pure prior shift (+0.40/−0.33)"
- issue: The two BoolQ gold-aligned margin components for qwen-1.5B are written without significance stars (+0.40/−0.33), whereas the neighboring models in the same sentence (gemma-12B, qwen-7B) carry stars on both components. Per source both qwen-1.5B components are significant, so the star omission is a shorthand inconsistency within the same sentence.
- fix: On line 243 of /home/jupyter/research/directed_kvcache_publication/paper_draft_v5.md, change "only qwen-1.5B shows the pure prior shift (+0.40/−0.33)" to "only qwen-1.5B shows the pure prior shift (+0.40\*/−0.33\*)" so both significant components carry stars, matching gemma-12B and qwen-7B in the same sentence.

## [MINOR] Abstract line 41-42: "priming helps Qwen-7B (−0.80 nats) but *hurts* the larger Qwen-14B (+0.44) and the Gemmas"
- issue: 'hurts ... the Gemmas' (plural) overstates the data. In the §7 QA table (line 446), Gemma-12B is +0.36* (significantly hurts) but Gemma-4B is +0.12 (n.s.) — Gemma-4B is not significantly hurt. Saying priming 'hurts the Gemmas' implies both, contradicting the section's own table where one of the two Gemmas is null.
- fix: In the abstract (line 46) change "hurts the larger Qwen-14B (+0.44) and the Gemmas" to "hurts the larger Qwen-14B (+0.44) and Gemma-12B (+0.36; Gemma-4B n.s.)". Apply the same correction to the parallel §7 sentence at line 476 ("...the larger Qwen-14B is *hurt* (+0.44), like the Gemmas") — change "like the Gemmas" to "like Gemma-12B (+0.36; Gemma-4B n.s.)" so both statements match the §7 table (line 468) rather than only the abstract.

## [MINOR] §3.3 line 202: "three pretrained **base** models (Gemma-4B-pt, Qwen-7B, Mistral-7B-v0.3)"
- issue: Naming collision. The base models for Gemma and Mistral carry disambiguating suffixes ("-pt", "-v0.3"), but the base Qwen is written as bare "Qwen-7B" — the exact token used everywhere else in the paper for the INSTRUCT model (imprintability table line 320, §6.1, §7, etc.). §6.4's table correctly disambiguates ("Qwen-7B base" vs "Qwen-7B instruct"), so §3.3's bare "Qwen-7B" in a base-model list is ambiguous/contradictory.
- fix: In the §3.3 base-model list (line 207), give the Qwen base a suffix consistent with the paper's own conventions. Best is "Qwen-7B-pt" to directly parallel the sibling "Gemma-4B-pt", giving "(Gemma-4B-pt, Qwen-7B-pt, Mistral-7B-v0.3)". Alternatively "Qwen-7B base" to match §6.4's table wording. Either removes the collision with the instruct-model name used elsewhere.

## [MINOR] §6.4 table, lines 355-360: code-bank column entries "Gemma-4B base +0.31 (no)", "Gemma-4B instruct +0.15 (no)", "Qwen-7B base +0.17 (no)", "Mistral-7B base −0.16 (code)"
- issue: Per the paper's stated convention (§3.3 line 206: "`*` excludes 0"), four code-column cells that are statistically significant in the source lack the significance star, while the two negative ones (Qwen-7B instruct −0.37*, Mistral instruct −0.56*) carry it. This is internally inconsistent: §6.1 correctly stars a significant POSITIVE value (Qwen rare_word +0.18*), so significant positives elsewhere should also be starred. As written, a reader cannot tell these base-model code effects are significant.
- fix: Add the significance star to the four significant code-column cells in the §6.4 table (lines 360-364), preserving the existing interpretation parentheticals: change "+0.31 (no)" → "+0.31\* (no)" (Gemma-4B base), "+0.15 (no)" → "+0.15\* (no)" (Gemma-4B instruct), "+0.17 (no)" → "+0.17\* (no)" (Qwen-7B base), and "−0.16 (code)" → "−0.16\* (code)" (Mistral-7B base). This makes the "`*` excludes 0" convention uniform across the table and the rest of the paper. (All four CIs from make_numbers.py exclude 0: +0.313 [+0.295,+0.331], +0.151 [+0.038,+0.265], +0.169 [+0.136,+0.202], −0.162 [−0.187,−0.139].)

## [MINOR] §4, line 235-237: "extract priming produces a *real* discrimination gain on the larger instruct models (gemma-12B ... ; qwen-7B +0.390*/+0.489*; ministral-8B +0.450*/+0.001) and improves accuracy, ECE, and Brier; only qwen-1.5B shows the pure prior shift"
- issue: Ministral-8B is grouped with the 'real discrimination gain' models, but its gold=no split is +0.001 (n.s.), i.e. one class up (+0.450*) and the other flat. By the paper's own §3.2 criterion (line 190-192: 'both classes up is genuine discrimination'; a one-sided move is not), Ministral-8B does NOT satisfy 'both classes up' — it is closer to a one-sided / partial-prior pattern than to the genuine-discrimination pattern that gemma-12B (+0.916/+0.782) and qwen-7B (+0.390/+0.489) exhibit. Listing it as a 'real discrimination gain' example is inconsistent with the section's own definition.
- fix: In §4 (line ~237) do not list ministral-8B as a full 'real discrimination gain' example alongside gemma-12B and qwen-7B. Instead note it as an intermediate/one-sided case and adjust the closing partition so it doesn't imply 'only qwen-1.5B' departs from full discrimination. Concretely, change '...qwen-7B +0.390*/+0.489*; ministral-8B +0.450*/+0.001) and improves accuracy, ECE, and Brier; only qwen-1.5B shows the pure prior shift (+0.40/-0.33).' to something like: '...qwen-7B +0.390*/+0.489*), both clearing the both-classes-up bar, and improves accuracy, ECE, and Brier; ministral-8B is an intermediate case (gold=yes +0.450*, gold=no +0.001 n.s. — one class up, one flat), improving balanced accuracy (0.850->0.863) without the symmetric up/down signature; only qwen-1.5B shows the pure prior shift (+0.40/-0.33).' This keeps the headline claim (discrimination sharpening on the two models that meet the criterion) while making ministral's partial/null gold=no explicit and consistent with §3.2.

## [MINOR] "machinery-neutral" (§3.2 line 193, §5 line 256, §9 line 552) vs "machinery-controlled" (§6.1 line 280, §7 line 441, §B line 636) vs "machinery-matched" (§7.1 line 483)
- issue: Three different adjectives for one concept (subtracting the content-free reposition+normalize construction cost via the newline prime). Only "machinery-neutral prime" is defined (§3.2 line 193). The measurement adjectives "machinery-controlled" and "machinery-matched" are never defined and never noted as synonyms of the neutral-prime baseline, so the reader must infer they all denote the same subtraction.
- fix: In §3.2, extend the machinery-neutral bullet with a defining clause that names the measurement adjectives, e.g.: "Machinery-neutral prime — a content-free, length-matched prime (newlines) isolates the reposition+normalize construction cost from the prime's content effect. Throughout, an effect reported as 'machinery-controlled' has this construction cost subtracted (measured relative to the machinery-neutral prime); a difference reported as 'machinery-matched' cancels the construction cost because both compared conditions share identical machinery (e.g., COND|sel in §7.1, where the retained set is held fixed)." Alternatively, standardize on a single adjective ("machinery-controlled") wherever the newline-prime baseline is subtracted (§6.1 line 285, §7 line 464, appendix line 665), while keeping "machinery-matched" only for the same-machinery difference at §7.1 line 506 since it is a genuinely different construction — but in that case still define both terms at §3.2.

## [MINOR] §3.3 line 201 and §4 lines 228-229 use "imprintability" quantitatively before its metric definition in §6.2 line 313 ("Define **imprintability** as the mean |Δ query-NLL| a generic prefix induces")
- issue: Term used before definition. Models are ranked by "imprintability" in §3.3 ("Eight instruction-tuned models spanning imprintability") and §4 splits results into "low-imprintability" vs "high-imprintability" models (lines 228-229) with quantitative force, but the metric is only defined in §6.2. The Abstract gloss (line 22-23, "recovers up to ~36%...") describes what imprintability predicts, not the metric itself, so it is not a definition. A reader hits the §4 low/high split with no operational meaning of the axis.
- fix: Add a one-clause operational gloss at the first quantitative use, §3.3 line 206, and forward-reference the full definition. Change "Eight instruction-tuned models spanning imprintability: Qwen 2.5..." to "Eight instruction-tuned models spanning imprintability (mean |Δ query-NLL| a generic prefix induces; defined §6.2): Qwen 2.5...". This grounds both the §3.3 ordering and the §4 low/high-imprintability split. Optionally, add the same short parenthetical at the Introduction's first bolded use (line 81) for readers who skip to it, though the §3.3 insertion is the load-bearing fix since it precedes the §4 quantitative split.

## [MINOR] §4, lines 242-246 (cascade paragraph) ending "...trust only high-powered estimates. We report these in §8."
- issue: The sentence promises §8 covers all three cascade casualties (contrastive/neighbor-subtraction inertness; the '85% leaked cacheable variant'; the N=300→400→900 replication flip), but §8 (lines 532-546) only reports the neighbor-subtraction inertness ('Contrastive priming is inert'). The 85%-leaked cacheable variant and the N=300/400/900 re-emergence story do not appear in §8, so the pointer is partially dangling.
- fix: Soften the §4 pointer so it references only the item §8 actually reports. Change the final sentence of the cascade paragraph (line 251) from "We report these in §8." to "We report the contrastive-priming result in §8." Alternatively (more invasive), add two bullets to §8 for the 85%-leaked cacheable variant and the N=300/400/900 replication flip so that the plural "these" is satisfied. The softening option is lower-risk and preferred since it does not require surfacing the underlying stats in §8.

## [MINOR] §7 line 450-452: "The mechanism is **not** semantic blurring. Shuffling the question's tokens barely changes the effect (order contributes ≤0.16 nats, mostly n.s.): what moves the answer is the **token presence** of the question"
- issue: The claim 'the mechanism is not semantic blurring' is stated categorically for all four models, but the §7 table's own 'order contribution' row shows Qwen-14B at +0.16* (significant, CI excludes 0). For Qwen-14B, order DOES contribute significantly, so 'the mechanism is not semantic blurring' is not universally true across the four models. The hedge 'mostly n.s.' is honest, but the leading categorical sentence overreaches for the one model where order is significant.
- fix: Soften the categorical lead at line 473 to name the exception explicitly, e.g.: "The mechanism is **predominantly token presence, not semantic blurring**: shuffling the question's tokens leaves most of the effect intact (order contributes ≤0.16 nats and is n.s. for three of four models — significant only for Qwen-14B, +0.16*, where even so the shuffled prime still carries the majority, +0.28 of +0.44). What moves the answer is chiefly the token presence of the question in the cached representation, with a sign that is model-and-scale-specific." This keeps the paper's token-presence framing while removing the unqualified "not semantic blurring" that its own table contradicts for Qwen-14B.

## [MINOR] Figure 3 caption, line 328: "predicts how much **semantic context** a model banks into a stripped cache"; and §6.2 code-block column "sem-bank" (line 318); and Figure 4 caption "a semantic context's value" (line 337-338)
- issue: Residual "semantic" vocabulary in figure captions/labels for the magnitude result that §6.5 reinterprets as token-presence banking for Gemma. The §6.2 body carefully hedges ("content banking", "the *kind* ... resolved in §6.5"), but the Figure 3 caption still says the trait "predicts how much semantic context a model banks," which reads as meaning-banking and clashes with §6.5's retraction of "Gemma banks meaning" (line 426).
- fix: In the Figure 3 caption (line 333), replace "predicts how much semantic context a model banks into a stripped cache" with "predicts how much content a model banks into a stripped cache (banking magnitude)" — matching the "content banking" language of the §6.2 body (line 318) and the magnitude framing of §6.2/§6.5. Optionally add a parenthetical "(kind of banking resolved in §6.5)" to mirror the inline caveat already present in the Fig 4 caption. The Fig 4 possessive "a semantic context's value" (line 342) can be left as-is since it describes the prime material and already carries a §6.5 caveat; the "sem-bank" code-block column header (line 323) is a compact label and is lower priority, but could be relabeled "content-bank" for full consistency.

## [MINOR] §6.2 code block, lines 318-322: header "imprint. sem-bank" with values e.g. "qwen1.5b 0.20 0.22 ... qwen7b 0.37 -0.14 ... gemma27b 0.84 3.77"
- issue: Sign-convention inconsistency. Every other banking table in the paper uses negative = banked (§6.1 line 280 "negative = banked"; §6.4 “−0.72→+0.14”; §6.5). This code block flips it to positive = banks: source-of-truth sem values are qwen1.5b −0.224, qwen7b +0.142, gemma27b −3.770, but the block shows +0.22, −0.14, +3.77 (i.e. −sem). The convention is never stated for this block, so a reader carrying the "negative=banked" convention from §6.1 will misread qwen7b's −0.14 (which actually means it does NOT bank, source +0.142) as banking. The column label "sem-bank" also retains the retracted "semantic" label for what §6.5 says is token-presence banking for Gemma.
- fix: In the §6.2 code block (paper_draft_v5.md lines 322-328), restore the paper-wide negative=banked convention so it agrees with §6.1/§6.4/§6.5 and with the §6.2 prose at line 337 (which already writes Mistral as −1.36). Show: qwen1.5b −0.22, qwen7b +0.14, qwen14b −0.01, mistral7b −1.36, gemma1b −0.57, gemma4b −2.46, gemma12b −3.62, gemma27b −3.77. (r=0.94 is invariant to the overall sign flip, so the correlation line is unchanged; note the y-axis in Fig 3/fig7 is plotted as positive=banks, so if the table is flipped, add a one-line note that the table uses negative=banked whereas the figure axis shows magnitude, or flip the figure to match.) Also rename the column header "sem-bank" to "content-bank" (or "banking") per the §6.2 reading-guide retraction of the "semantic" label, and add an explicit one-line convention note above the block, e.g. "(banking in nats; negative = banked, matching §6.1)". Keeping the block's current positive-magnitude numbers is the alternative only if the note is added AND the contradictory prose "−1.36" at line 337 is also reconciled — flipping the block to the paper convention is cleaner.

## [MINOR] §3.3 line 201-203 ("Eight instruction-tuned models spanning imprintability: Qwen 2.5 (1.5/7/14B), Mistral 7B, Gemma 3 (1/4/12/27B)") vs §7.1 table line 490 ("Qwen-3B  | 0.30 | ...")
- issue: Two different 'eight models' sets are used without reconciliation. §3.3's declared roster is {Qwen-1.5B/7B/14B, Mistral-7B, Gemma-1B/4B/12B/27B} — it contains NO Qwen-3B and DOES contain Gemma-27B. But §7.1's 'eight models' set is {Qwen-1.5B/3B/7B/14B, Gemma-1B/4B/12B, Mistral-7B} — it INCLUDES Qwen-3B and EXCLUDES Gemma-27B. So §3.3 fails to list Qwen-3B even though a headline result (§7.1) depends on it, and 'eight models' silently denotes two different rosters in §6.2 vs §7.1.
- fix: In §3.3, add Qwen-3B to the instruction-tuned roster so every model that appears in a results table is introduced — e.g., change "Qwen 2.5 (1.5/7/14B)" to "Qwen 2.5 (1.5/3/7/14B)" and note that this yields nine instruction-tuned models total, of which any given analysis uses eight. Then add one clarifying sentence (in §3.3 or as a footnote at the first §7.1 use of "eight models") stating that the select-vs-condition analysis (§7.1) substitutes Qwen-3B for Gemma-27B relative to the banking/imprintability roster (§6.2, §6.5), so "eight models" denotes two overlapping-but-distinct rosters. Alternatively, if Gemma-27B and Qwen-3B were both actually run for §7.1 and one was merely omitted from the printed table, add the missing row so §7.1 matches the declared roster.

## [MINOR] §7 title (line 430) "## 7. Mode–Task Match: When Imprinting Helps" and opener (line 432) "Content imprinting is not uniformly good — its value depends on the task"; vs §7 body line 456 ("demote the mode–task '2×2' ... to an *illustrative* worked pair") and line 454-455 ("the earlier clean story ... is **not a law**")
- issue: The section is titled 'Mode–Task Match' and asserts a task-dependent match, but the section's own conclusion is that the mode–task match is NOT a law and is demoted to an illustrative single pair. The title therefore asserts the very finding the section retracts, which will mislead a skimming reader into thinking a mode–task law is established.
- fix: Retitle §7 to match the paper's own hedged framing (which already scare-quotes "match" and stresses "limits" at lines 43 and 86). Suggested: `## 7. Downstream Value and the Limits of a Mode–Task Rule` or `## 7. When Does Imprinting Help? The Limits of a Mode–Task "Match"`. Optionally soften the opener at line 454 to foreshadow the retraction, e.g. "Content imprinting is not uniformly good — its value depends on the task, but the earlier mode–task 'match' turns out to be model-specific, not a family law (below)." No changes to lines 43/86 are needed; they already use the correct hedged phrasing.

## [MINOR] Related Work, 'Task-aware and trained caches' (lines 132-134): "Our contribution to this thread is the *mode* account: which conditioning content a given model can bank is governed by a measurable trait (§6.2), so the right task-aware prime is *model-specific* — a prescription these retention-based methods do not provide."
- issue: Related Work still advertises 'the *mode* account' and implies a model-specific task-aware *prime prescription* as the contribution versus prior work — but §6.5 retracts the mode framing and §7.1 withdraws the model-specific prescription (r=0.29, 'probe both'). Related Work is thus over-claiming relative to the corrected body, and specifically over-differentiating from Beyond RAG/SnapKV on a basis the paper no longer supports.
- fix: Rewrite the last sentence of the "Task-aware and trained caches" paragraph (lines 137-139) to match the corrected body, dropping both "the mode account" as the advertised contribution and "the right task-aware prime is model-specific" as a delivered prescription. Suggested replacement: "Our contribution to this thread is two-fold: (i) a *what-kind* axis on top of prior *how-much* work — imprintability predicts banking *magnitude* (§6.2), and a shuffle control shows the *kind* of content banked is model-specific (token presence for Gemma vs. structure for Mistral; §6.5); and (ii) making task-aware *selection* a first-class, iso-budget baseline against discarded-prefix conditioning (§7). We find that neither conditioning nor selection dominates and that which wins does not reduce to a single trait (r=0.29), so the deployable prescription is to *probe both per model* — while noting the one systematic asymmetry these retention-based methods should heed: aggressive query-aware selection carries a Qwen-family-specific risk that conditioning sidesteps." This keeps the genuine differentiation (what-kind axis; selection-as-baseline; the Qwen selection risk) while removing the withdrawn mode/model-specific-prime prescription.

## [MINOR] §7 header (line 430) "## 7. Mode–Task Match: When Imprinting Helps" and §7 body demotion (lines 456-458): "We therefore demote the mode–task '2×2' below to an *illustrative* worked pair, and replace the prescription with the eight-model select-vs-condition comparison of §7.1"; plus the 2×2 table still printed at lines 463-466.
- issue: The section is titled 'Mode–Task Match' but its own text says the mode-task match 'is not a law,' 'demotes' the 2x2, and the shuffle controls show the QA effect is token presence, not mode. Keeping the section title and the 2x2 table while the prose disowns them is a course-correction left half-applied: the reader sees a promoted artifact (title + table) that the text simultaneously demotes. This is the 'lab journal with visible edits' symptom.
- fix: Retitle §7 so the header matches its (already-corrected) content — e.g., "## 7. Downstream Value and the Limits of a Task-Aware Rule" or "## 7. Downstream Value: Reranking, QA, and Task-Aware Construction." This is the load-bearing fix, since the section title is the one promoted, uncaveated element still asserting the retracted "Mode–Task Match / When Imprinting Helps" claim. The 2×2 table (l.486-489) is optional to touch: it is already labeled "*not* a family law" in its own header (483-484) and Figure 6 caption (492-495), so it may simply be left as the caveated illustrative inset, or moved into a clearly labeled inset/cut if space is tight, since §7.1's eight-model table supersedes it. No ToC exists to update (section headers are the only navigational anchors), and the abstract/intro already use the corrected framing, so no further edits are needed for consistency.

## [MINOR] Figure ordering: "![Figure 7]" at line 410 (inside §6.5) precedes "![Figure 6]" at line 468 (inside §7)
- issue: Figures appear out of numerical order in the document: the sequence in reading order is Fig 1,2,3,4,5,7,6,8. Figure 7 (shuffle controls, fig12) is placed in §6.5 before Figure 6 (mode-task, fig10) in §7. Journals require figures numbered in order of first appearance. There is no in-text 'Figure 6'/'Figure 7' callout to disambiguate (only 'Fig. 3–4' is referenced in-text), so the mismatch is purely the caption numbers.
- fix: Swap the two caption numbers so figures are numbered in order of appearance. In §6.5 (line 432), change "*Figure 7:" to "*Figure 6:" for the shuffle-controls figure (figures/fig12_shuffle_controls.png). In §7 (line 492), change "*Figure 6:" to "*Figure 7:" for the mode-task figure (figures/fig10_mode_task.png). Also update the two Markdown image alt tags for consistency: line 431 "![Figure 7]" -> "![Figure 6]" and line 491 "![Figure 6]" -> "![Figure 7]". No numeric in-text cross-references to these two figures exist, so no other edits are needed (the "(§6.5)" reference at line 495 is a section reference and stays as is).

## [MINOR] §7 Reranking paragraph (lines 435-438): "beats *no* priming on the larger, higher-imprintability models: **+0.036 MRR on Gemma 12B and 27B** (CIs exclude 0), null on Gemma 4B and on Qwen/Mistral."
- issue: Narrative-fairness / robustness gap the reader will notice: the reranking win is asserted as a clean positive for the token-presence account, but §6.5's own logic (Gemma banks token presence, order-invariant) is not tested here — the paper elsewhere is scrupulous about shuffle controls yet the one *downstream* positive result (reranking) is presented without the shuffle lens that the paper makes central. (The reranking-shuffle run is noted as in-flight and excluded from flagging per instructions, but the current text's confident causal gloss 'keyword imprinting re-weights the passage's own salient content ... for Gemma this is a token-presence re-weighting; §6.5' asserts the mechanism it has not yet measured for this task.)
- fix: Soften the parenthetical mechanistic gloss on line 461 from an assertion to a hypothesis pending the reranking shuffle run. Change:

"which is exactly what relevance scoring rewards (for Gemma this is a token-presence re-weighting; §6.5)."

to:

"which is exactly what relevance scoring rewards (we expect this to be a token-presence re-weighting for the Gemma family, per §6.5; a reranking-specific shuffle control (exp34, tfidf_shuffled) is in progress)."

This keeps the model-level generalization from §6.5 (legitimate) while making clear the reranking-task shuffle has not yet been measured — matching the standard the paper applies to QA (which does report an explicit task-specific shuffle). Optionally add a one-line note to §10 Limitations that the reranking mechanism (token-presence vs structure) is inferred from the §6.5 model-level trait and awaits a task-specific shuffle control.

